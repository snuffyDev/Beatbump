{
	"$schema": "http://json-schema.org/draft-07/schema",
	"type": "object",
	"properties": {
		"environment": {
			"description": "Deployment configuration (Node, Cloudflare Workers, Vercel, etc..)",
			"type": "string",
			"enum": ["node", "workers", "vercel", "netlify"]
		}
	}
}
