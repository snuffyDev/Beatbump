// import type { JSON } from "$lib/types";

// const HISTORY_MAP = new Map<string, JSON>();

// const $$snapshot = () => {};
// export default (() => {
// 	if (typeof history !== "undefined") {
// 		// sessionStorage.
// 		console.log(history.state);
// 	}
// })();

export default void 0;
